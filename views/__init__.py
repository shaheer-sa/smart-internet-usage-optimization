# views/__init__.py
# Makes the views/ directory a Python package so modules can be
# imported with:  from views.page_home import render
#
# NOTE: This folder is intentionally NOT named 'pages/' to prevent
# Streamlit's native multipage system from auto-intercepting it.
